# UI Module
**Description:** Simple visual components for terminal-based UI rendering.
### Features:
- `gage_draw_box()`: Renders a block `[]` on the screen.
- `gage_draw_line()`: Renders a horizontal divider line.
### How to Use:
1. Import: `import ui`
2. Draw a line: `exec "gage_draw_line();"`
