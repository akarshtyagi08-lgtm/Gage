# Time Module
**Description:** Interacts with the system clock.
### Features:
- `gage_get_timestamp()`: Returns the current UNIX timestamp in seconds.
### How to Use:
1. Import: `import time`
2. Store time: `let current_time = gage_get_timestamp()`
3. Useful for measuring how long a loop takes!
