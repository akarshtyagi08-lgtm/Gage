# 🌐 Comprehensive Network Module
Diagnostic sockets analyzers, routing tracer boards, configuration trackers, and address mappers.

### Complete Feature List:
1. `gage_net_ping_dns()` - Dispatches real-time ICMP test probes to public networks.
2. `gage_net_curl_headers()` - Extracts network header blueprints using curl interfaces.
3. `gage_net_wget_spider()` - Audits target endpoints availability avoiding payload downloads.
4. `gage_net_print_interfaces()` - Maps physical adapter addresses directly onto terminal readouts.
5. `gage_net_print_sockets()` - Audits active ports holding socket configurations listening tracks.
6. `gage_net_print_routes()` - Captures routing charts tracking local gateway connection trajectories.
7. `gage_net_traceroute()` - Tracks operational nodes hopping sequences across gateways maps.
8. `gage_net_dns_lookup()` - Translates human-readable server title strings into network IP coordinates.
9. `gage_net_whois_query()` - Scans registry parameters records to verify domain leasing profiles.
10. `gage_net_print_arp_table()` - Maps neighboring physical node MAC resolution coordinates instantly.
11. `gage_net_print_local_ip()` - Reports active internal subnet tracking targets addresses maps.
12. `gage_net_print_public_ip()` - Contacts remote echo arrays to isolate out-of-boundary public IPs.
13. `gage_net_scan_local_ports()` - Scans system listening gates to diagnose visibility loops.
14. `gage_net_iptables_list()` - Exposes firewall sorting criteria matrices (requires system privileges).
15. `gage_net_ufw_status()` - Validates whether foundational network protection walls are deployed.
16. `gage_net_curl_geo_ip()` - Parses remote database streams converting IP tags to geolocation coordinates.
17. `gage_net_ping6_dns()` - Routes diagnostic packages over next-generation protocol links channels.
18. `gage_net_print_mac_addresses()` - Captures absolute physical machine address footprint tracking rows.
19. `gage_net_curl_user_agent()` - Audits system interface context descriptors formatting metadata profiles.
20. `gage_net_curl_check_http2()` - Confirms whether multi-stream binary communication modes are operational.
21. `gage_net_print_statistics()` - Summarizes total socket byte data transactions profiles charts.
22. `gage_net_print_active_connections()` - Maps running transaction pipeline allocations in real-time memory blocks.
23. `gage_net_test_dns_latency()` - Profiles link variation metrics using multiple timing sweep points.
24. `gage_net_check_port_ssh()` - Verifies if administrative secure shell channels are accessible locally.
25. `gage_net_check_port_http()` - Verifies if standard localized web development testing ports are online.
26. `gage_net_print_network_protocols()` - Dumps standard structural transport layouts references pages.
27. `gage_net_print_network_services()` - Reports system registered port indicator purpose descriptions rows.
28. `gage_net_clear_dns_cache_mock()` - Emulates clearing corrupted address validation tracks files nodes.
29. `gage_net_reset_interface_mock()` - Simulates linker adapter state reboots to trace reconnection loops.
30. `gage_net_print_wireless_info()` - Extracts local wireless node parameter data directly from system streams.
